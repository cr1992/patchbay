name: patchbay_repo
description: Private repository automation for the Patchbay workspace.
publish_to: none

environment:
  sdk: '>=3.12.0 <4.0.0'

workspace:
  - packages/patchbay
  - packages/patchbay_cli
  - packages/patchbay_flutter
  - packages/patchbay_transport

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.0
