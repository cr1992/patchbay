.dart_tool/
build/
*.iml

# pubspec_overrides.yaml 不再忽略：随版包在 pubspec.yaml 里已是 hosted 约束（发布合法），
# 仓内解析到工作树全靠这些 overrides，必须入库。由 release_prep --apply 维护。

# 本地代码索引（工具生成，勿入库）
.codegraph/

# 接入方真机验收证据（含业务应用名称、包名与设备标识，按 AGENTS.md 的保密规则不得入库）。
# 需要留存时放在这里，评审引用结论而不是文件。
.local/
