.dart_tool/
build/
*.iml
/pubspec.lock

# 四个发布包由根 pub workspace 解析；workspace 外的 example 保持独立 consumer lock，
# 根 lock 与发布包 lock 一样按 SDK 组合现解，避免当前 SDK 锁阻断 sdk_floor；example
# 的 consumer lock 仍入库。它的 pubspec_overrides.yaml 由私有 release task 维护。

# 本地代码索引（工具生成，勿入库）
.codegraph/

# 接入方真机验收证据（含业务应用名称、包名与设备标识，按 AGENTS.md 的保密规则不得入库）。
# 需要留存时放在这里，评审引用结论而不是文件。
.local/
.worktree-trace.json
