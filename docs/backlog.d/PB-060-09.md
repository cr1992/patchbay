---
id: PB-060-09
title: Semantics 按 nodeId 派发的拒绝 details 与 identifier 路径对齐并补 enabled
target: 0.6.0
status: 实现中
---

## 动机

`packages/patchbay_flutter/lib/src/semantics/semantics_bridge.dart` 的 `_resolve`（nodeId 路径）对 `uiSemanticsActionBlocked` 与 `uiSemanticsActionUnavailable` 返回裸码，`uiSemanticsGenerationStale` 只带 `currentGeneration`，同文件 `_resolveIdentifier` 对同一组码带 nodeId、generation、label、actions、invisible、userActionsBlocked 与 requestedAction；接入方真机在浮层下点禁用按钮只拿到 `uiSemanticsActionUnavailable`，得截屏才知道是按钮禁用，而语义树本就带 `hasEnabledState` / `isEnabled`

## 备注

[Semantics 拒绝 details 对齐](../proposals/0.6.0/semantics-rejection-details.md)（已接受）；DG-060-06 已裁决候选表七键封闭、`enabled` 只在节点声明 enabled 状态时出现、不新增 reason 词表；纯 additive，错误码、退出码与 VM/direct 行为不变；仓内单测已绿，真机验收待接入方
