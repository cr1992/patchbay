---
id: PB-050-07
title: semantics probe 请帧策略与 identifier 索引
target: 0.5.0
status: 实现中
---

## 动机

减少主动请帧或按 tree revision 缓存 identifier 都会改变 `ui.wait` 的观察时机、elapsed/frameRevision 与 generation 边界，不能作为“透明降载”越过默认行为门禁

## 备注

[Semantics probe 调度](../proposals/0.5.0/semantics-probe-scheduling.md)（已接受）；DG-050-05 已裁决：one-shot 零额外帧、frameRevision 全计入、index 本版不做（阈值 20% 或 P95>2ms）；请帧策略与 `ui.wait` cadence 已实现并由帧计数断言冻结，Android 真机 debug 预检 106/0 与 profile smoke 17/0 已跑，接入方验收随 RC 矩阵
