---
id: PB-050-38
title: 超级 bridge 按状态机阶段拆分
target: 0.6.0
status: 实现中
---

## 动机

五个热点当前行数已用 `wc -l` 实测:`packages/patchbay_flutter/lib/src/semantics/semantics_bridge.dart` 969 行、`packages/patchbay_flutter/lib/src/flutter_service_host.dart` 949 行、`packages/patchbay_flutter/lib/src/reveal/reveal_engine.dart` 788 行、`packages/patchbay/lib/src/host/host_invoker.dart` 699 行、`packages/patchbay/lib/src/host/snapshot_payload.dart` 613 行;semantics bridge 单类内堆叠采样(`observeTreeRevision`,184 行起)、identifier 解析(`_resolveIdentifier`/`_resolve`,512/624 行起)、generation 追踪(`_treeRevision`/`pinnedGeneration`)、policy 决策(`_actionPolicy`,348-349,373-379,417-419 行)、gate 评估(`_gates.evaluate`,210-211,355-358,403-404 行)、遮挡判定(`obscured`/`isObscured`,166,379,426 行)与 action dispatch(`_dispatch`,338-509 行,内部 resolve→gate→policy→遮挡判定→再 resolve→再 policy→dispatch 顺序执行)七类职责堆叠的事实。方向:按状态机阶段拆(resolve target → preflight fence → policy/gate → post-gate revalidation → dispatch → evidence/audit projection),各阶段独立输入输出、可独立失败注入——不按行数机械拆

## 备注

与结构警戒线治理呼应但独立;大规模拆分明确不进 0.5.0(评审与仓主一致意见);1/5：semantics bridge 已按阶段拆分，其余四个热点待后续 MR;3/5：host invoker 已按 admission pipeline 阶段拆分，其余热点由各自 MR 处理;2/5：flutter service host 已按职责拆分，其余热点待后续 MR
