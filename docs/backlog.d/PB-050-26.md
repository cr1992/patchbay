---
id: PB-050-26
title: 审计事件的 reveal 富化
target: 0.6.0
status: 已验证
---

## 动机

`ui.reveal` 的审计事件缺 steps 与被驱动容器 nodeId 列表；补齐需改 `PatchbayAuditEvent` 公共形状与 host_invoker 记账，越出 PB-050-17 授权面

## 备注

[Gate 声明与执行真源](../proposals/0.6.0/gate-execution-boundary.md)（已接受）与 [DG-060-04](../backlog.d/DG-060-04.md) 已接受富化：core 仅从 schema 已校验的 accepted reveal payload 投影 `steps` 与最多 200 个 `containerNodeIds`，不含 identifier、坐标、文案或 policy 文本。该前提要求 `ui.reveal` descriptor 自身声明 `responseSchema`（本条目一并落地，`schemaMode` 随之变为 `validated`）；schema 无法表达的语义不变式（`steps == 0` 时容器列表为空、nodeId 去重）留在 host 投影器按 defect 通道处理；候选 eb9176db 的 example 预检 99/99 通过（../verification/0.6.0-candidate-example-precheck.md）；audit 投影由 host 单测与 VM/direct 对拍钉住，接入方 auditSink 观察待做
