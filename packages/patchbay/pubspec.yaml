name: patchbay
description: Consumer-neutral protocol and admission primitives for Flutter runtime debugging.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  test: ^1.25.0
