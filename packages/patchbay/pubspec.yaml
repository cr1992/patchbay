name: patchbay
description: 'Typed control channel into a running Dart or Flutter app: protocol, gated command admission, fact-sourced state, jobs and blobs.'
repository: https://github.com/cr1992/patchbay
version: 0.2.1
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dependencies:
  crypto: ^3.0.6

dev_dependencies:
  test: ^1.25.0
