name: patchbay_transport
description: Consumer-neutral, explicitly enabled direct debug transport for Patchbay.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.11.0 <4.0.0'

dev_dependencies:
  test: ^1.25.0
