- `docs/quickstart.md` 补两处此前无先验 Agent 量测暴露的缺口：仓内示例 App 出仓不带平台工程目录，
  step 1 加入 `flutter create --platforms=...` 的生成与副作用（`pubspec.lock`/`test/widget_test.dart`）
  恢复说明；新增 "Non-interactive path (scripts and agents)" 小节，指向仓内 `tool/example_session.sh`
  作为脚本/Agent 场景下非交互起 App、取会话句柄的方式；该脚本被 zsh source 时也能自定位仓库根，
  定位失败当场报错并提示 `PATCHBAY_REPO_ROOT` 覆盖，不再带着错的目录继续跑。
