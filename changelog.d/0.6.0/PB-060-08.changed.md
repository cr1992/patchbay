- canonical `ui perform` 家族不再提供 `set-text`：`ui text set` 的迁移目标改为 `ui perform enter-text`（依次执行目标的
  `inputFormatters` 与 `onChanged`）；只写 controller、不通知业务回调的 `ui.text.set` 仍可经 deprecated 的
  `ui text set` 使用至 1.0，wire 命令与 host 行为不变。`ui perform` 的 help 同时标明 `target:` 的 generation 来自
  `ui targets`、`semantics:` / `node:` 的来自 `ui semantics tree`，stale 拒绝携带 `currentGeneration` 但 CLI 不自动重发。
