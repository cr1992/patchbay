- `doctor` 的 session 检查不再把隔离文件的本机绝对路径写进 `details.quarantinedFiles`，改为只报会话目录内的
  文件名。操作者仍能定位文件（会话目录由自己的 `--session-dir` 或默认值决定），而 doctor 输出被粘进 issue 时
  不再带出本机路径；取不到文件名时报 `<redacted:absolute-path>` 而非退回原值。与 workspace root 从不进该
  finding、以及 trace 对绝对路径的既有脱敏口径一致。
