- Linux 会话探针现在会先证明 procfs 与 CLI 使用同一 PID namespace，避免 bind-mounted 外来
  procfs 在数字 PID 碰撞时误判进程存活或启动身份；外来 procfs 下不会再回退同样读取它的 GNU `ps`；
  会话启动签名的写入与读取同时改用同一套严格
  codec，无法由平台探针产生的日期和 tick 载荷一律降级为身份未核实，不再误删运行中的会话记录。
