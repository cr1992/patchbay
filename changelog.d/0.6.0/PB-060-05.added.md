- 新增英文 [`docs/quickstart.md`](docs/quickstart.md)：只用仓内示例 App，10 分钟内走完
  install→`identity`→`catalog`→`snapshot`→`ui perform` 安全写操作→`capture` 六步，每步标注可见结果与
  失败时应查的 `error.code`；README（中英）Quick Start、`skills/use-patchbay/INSTALL.md` 与
  `SKILL.md` 均把它接为渐进披露的第一层入口，README 示例中残留的旧版 `ui tap` 写法一并替换为 canonical
  `ui perform`。新增
  [`docs/verification/0.6.0-onboarding-funnel.md`](docs/verification/0.6.0-onboarding-funnel.md)
  记录模板，固定无先验用户/Agent 接入漏斗的用时、失败点与实际读取层级字段，量测结果留待后续独立执行。
