- `ui.reveal` 目录条目新增 `responseSchema`，声明受理 payload 的 `steps`（integer）与 `containers`
  （每项含 integer `nodeId`）并显式开放 `additionalProperties`，其余 0.5.0 冻结键继续走松读面。该命令的
  `schemaMode` 因此从 `legacyUnvalidated` 变为 `validated`（逐命令，不逐 host）：这两个字段缺失或类型
  错误的 provider 应答从此在 host 与 CLI 两侧稳定拒绝为 `providerProtocolViolation`，而不再被静默受理。
  `catalogDigest` 的 `covers` 不变，老 CLI 忽略该 additive 目录键，reveal 的 payload 字节与稳定拒绝码
  均未改变。
