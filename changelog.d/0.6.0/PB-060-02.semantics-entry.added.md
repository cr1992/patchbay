- `package:patchbay_flutter/patchbay_flutter_host.dart` 导出 `PatchbaySemanticsEntry`。它一直是
  `PatchbaySemanticsBridge.observe` 的返回类型，却从未被任何公共入口导出，因此 0.5.x 的接入方无法为该调用
  写出显式类型标注。0.6.0 的入口自足规则要求公共签名里的类型必须可命名，这个缺口随之补上。
