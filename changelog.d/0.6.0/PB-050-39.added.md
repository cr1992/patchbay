- `PatchbayAuditEvent` 新增可选 `admissionStage` 与 `gateDisposition`，如实记录一次调用在准入流水线上
  停止或完成的阶段，以及相对接入方声明门的处置。两者是 host-only 事实，不进 invocation 信封也不进
  rejection details；缺失时 `toJson` 省略该键，`gateResult` 与既有键逐字节不变，旧源码构造该事件仍可编译。
  新 reader 读到老 host 缺键时按 `patchbayAuditLegacyUnknown` 处理，**不得**把缺失推断为已通过。
