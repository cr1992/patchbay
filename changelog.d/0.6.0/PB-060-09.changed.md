- `ui.semantics.action` 按 nodeId 派发时，`uiSemanticsActionUnavailable`、`uiSemanticsActionBlocked` 与
  `uiSemanticsGenerationStale` 的拒绝 details 与 identifier 路径同形；两条路径的候选节点 details 在节点声明
  enabled 状态时新增 `enabled`，禁用按钮的拒绝不再只有裸码。老 reader 忽略新增键，错误码与退出码不变。
