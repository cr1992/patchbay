- `docs/quickstart.md` 不再在 `identity` 一步承诺一份它给的命令产不出的输出：人读形态就是
  `<applicationId> instance=<id>` 一行，完整握手字段（`appInstanceId`、`isolateId`、`schemaVersion`、
  `serverVersion`、`features`）改为明确指向 `--json`；非交互路径补两条执行器现实——helper 的
  `[session]` 状态行是中文的进度与告警、不带 `error.code`，以及每条命令都起新 shell 的执行器
  （多数 Agent 宿主如此）要如何接住 `PATCHBAY_SESSION_ID`（`PATCHBAY_SESSION_DIR` 会按检出自行推导）。
